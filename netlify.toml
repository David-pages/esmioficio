[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: todas las rutas las resuelve index.html (React Router en el cliente)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
